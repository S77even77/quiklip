2026-08-06 | built quiklip: phone-UI + Mac-side ffmpeg long-form video clipper (mark-as-you-go, multi-screen nav, batch export) | verified: frame-exact cuts confirmed against burned-in timecode (10.000/47.500/1:40.000 exact), reframe dims correct for 9:16+1:1+4:5, rotation display-matrix handled, 11-step headless iPhone-viewport walkthrough passed with 0 console errors, zip + persistence working | verdict: pending
2026-08-07 | pivoted quiklip to a 100% client-side static app (ffmpeg.wasm + WORKERFS mount, IndexedDB, dep-free zip) for free Vercel hosting; kept Mac-server version in legacy-local-server/ | verified: npm run build passes from a CLEAN CLONE (24 pkgs, 62MB dist w/ both cores); headless-Chromium E2E pushed a real file in and ffprobed the output — 2 clips exact 5.000s, 406x720, burned-in timecodes read 10.0/14.9/47.5/52.4; 10s of video encoded in 0.9s wall-clock; crossOriginIsolated+SharedArrayBuffer true; repo pushed to github.com/S77even77/quiklip (private); NOT deployed (vercel cli unauthenticated) | verdict: pending
2026-08-13 | added in-app camera recording (getUserMedia + MediaRecorder -> same File/store/clip/export pipeline as uploads); fixed a real bug found in the process: -movflags +faststart was unconditional and crashed Lightning-mode export of webm sources with "Unrecognized option" | verified: headless Chromium with --use-fake-device-for-media-stream recorded 3s of synthetic video for real, reviewed it, used it, landed in Clip at correct 1920x1080/3.58s, marked+exported a clip -> ffprobe confirmed h264 854x480 3.30s; movflags fix confirmed directly via buildArgs() unit check (absent for .webm output, present for .mp4); screenshots of all 5 steps visually reviewed | verdict: pending
2026-08-13 | added pinch-to-zoom (canvas redraw pipeline, affects recorded output not just preview) + live quick-clip button (small button next to record, saves current segment straight to Clips tab while recording keeps going via chained MediaRecorder segments); attempted native-picker mitigation (accept=extensions-only, no video/* wildcard) to reduce iOS showing "Take Video" -- UNVERIFIED, no physical iPhone to test, flagged to user; fixed a review-screen duration race (0:00 flash) surfaced by testing | verified: headless Chromium w/ fake camera -- synthetic 2-finger pinch via real PointerEvents changed zoom 1.0x->2.4x AND visibly altered the composited frame (not just the indicator); quick-clip tapped twice mid-recording, timer kept counting (0:02->0:04) without reset, both segments landed in Clips as independently valid ffprobed h264 clips; final tail segment still flows through review->Library->Clip screen unchanged; original upload/export E2E suite re-run clean (regression check) | verdict: pending
2026-08-13 | fixed real bug: quick-clip button not appearing while recording on a real device -- root cause was an unguarded startRecording() where canvas.captureStream()+MediaRecorder(canvas+mic) could throw and silently abort the whole function before any UI update ran; added try/catch with automatic fallback to raw-camera recording (zoom becomes preview-only in that case) plus a shared endRecordingUI() cleanup used by both normal and error paths; also merged the two Library add-buttons into one "Add a video" button opening an in-app choice sheet (Record / Choose), since iOS still cannot suppress its native Take Video option (no web API exists, confirmed last session) -- reducing to one button was the achievable part of that request | verified: forced captureStream to throw in headless Chromium (simulating the suspected real-device failure) -- quick-clip button still appeared, recording still worked, 4 valid clips ffprobed across the full run; original quick-clip/zoom/upload/export suites re-run clean after the refactor; caught and fixed a stale "Choose a video" empty-state string left over from the merge | verdict: pending
2026-08-13 | added a real logo mark next to the quiklip wordmark: gradient badge with a play-triangle notched like it was clipped, two-tone "qui|klip" wordmark (klip in the accent gradient + soft glow, reusing the existing REC-dot glow language rather than importing a new effect); updated the favicon/app-icon to match for a consistent identity everywhere it appears | verified: screenshotted the isolated mark at 3x zoom to check the hand-authored SVG notch geometry actually reads as a clean cut rather than a broken shape, then the full header in context against the rest of the UI; full upload/export and recording/zoom/quick-clip regression suites re-run clean, zero console errors | verdict: pending
2026-08-13 | fixed critical bug: recording produced all-black video on real device -- root cause was the zoom feature's hidden 2x2px off-screen <video> feeding a canvas redraw loop, a pattern iOS Safari is known to throttle/stop decoding on; removed the whole canvas-compositing pipeline and reverted to the proven approach (record the raw camera stream directly, exactly like before zoom existed); zoom is now a live CSS transform on the visible preview -- still works as a framing aid while filming, but no longer bakes into the saved file (deliberate reliability tradeoff given the alternative was a broken core feature); also added camPreview.play() called explicitly rather than relying solely on the autoplay attribute | verified: bundle size dropped (39.60KB vs 40.44KB) confirming real code removal; full recording suite re-run clean; added a genuinely new regression guard -- assertNotBlack() decodes actual frames and averages raw RGB bytes (not ffmpeg signalstats/YAVG, which gave inconsistent numbers against manually-verified frames and wasn't worth chasing further) -- both the quick-clip segment and exported clip confirmed non-black (46.3/255, 52.2/255); CSS-transform zoom visually confirmed still zooms the preview (screenshot at 1x vs 2.8x); IMPORTANT CAVEAT: the fake camera in headless Chromium never exhibited this bug even with the old broken code, so this suite cannot prove the real iOS Safari failure is fixed, only that the new code is sound and produces valid non-black output in a normal browser -- flagged to user, asked them to re-verify on device | verdict: pending
